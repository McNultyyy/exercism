name: .NET Core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fsharp

    steps:
    - uses: actions/checkout@v2
      
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301

    - name: Install dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build --configuration Release --no-restore
    
    - name: Creat neccessary directories
      run: |
        mkdir "_TMP" 
            
    - name: use this action, test solution dir
      uses: zyborg/dotnet-tests-report@v1.0.0
      with:
        report_name: my_project_tests
        report_title: My Project Tests
        github_token: ${{ secrets.GITHUB_TOKEN }}
    # - name: Test
    #  run: |
    #    for directory in */ ; do mkdir "$directory/TestResults"; done
    #    dotnet test --no-restore --verbosity normal --logger "html;LogFileName=logFile.html"
